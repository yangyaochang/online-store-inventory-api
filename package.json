{
  "name": "inventory-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch inventory.json --port 3003",
    "server": "nodemon server.js",
    "heroku": "NODE_ENV=production node index"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "14.4.0"
  },
  "dependencies": {
    "json-server": "^0.16.1",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
